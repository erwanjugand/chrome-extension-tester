console.log('panel script loaded')

// chrome.devtools.inspectedWindow.getResources(ressources => console.log(ressources))
// const [ r1, r2, r3, r4, r5, r6, r7 ] = await chrome.devtools.inspectedWindow.getResources()
// r7.getContent((content, encoding) => console.log(content.slice(0, 100), encoding))
// await r7.getContent()

// r7.setContent('content', true, (result) => console.log(result))
// r1.setContent('content', true, (result) => console.log(result))
// await r7.setContent('content', true)
// await r1.setContent('content', true)
